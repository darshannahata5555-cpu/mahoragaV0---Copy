# AI
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-pro

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/ytpipeline

# Redis / Celery
REDIS_URL=redis://localhost:6379

# App
PORT=8000
FRONTEND_URL=http://localhost:3000
OUTPUT_DIR=./outputs

# Auth (placeholder — not enforced in V0 but reserved)
APP_API_KEY=
